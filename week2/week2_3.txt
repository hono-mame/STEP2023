キャッシュの管理をほぼ O(1) で実現できるデータ構造??

（(URL, ページ名),URLのハッシュ値)の要素が最大X個の数値のリストを作る。
[ ((URL, ページ名),URLのハッシュ値), ((URL, ページ名),URLのハッシュ値),......]
新しいデータを入れた際に、
・ハッシュ値を計算し、((URL, ページ名),URLのハッシュ値)を作る。
・リストを見ていき、同じものが見つかった場合は、その要素を削除し、最後にその要素を追加。（移動させる）
・リストの中に見つからなかったら、リストの先頭のタプルについて、deleteを実行する。（同じハッシュ値の中では線形探索(多分被らない...?)）
  また、リストの最後に新しいデータの情報を追加し、ハッシュ値を元にputを行う。

この操作をすることで、X個のデータのみをハッシュテーブルの中に記録しながら、その順序まで考えることができる？ 全然わからない、、、、